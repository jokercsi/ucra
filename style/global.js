import { StyleSheet } from "react-native";

export const globalStyle = StyleSheet.create({
  container: {
    padding: 12,
    marginBottom: 130,
  },
  titleText: {
    //fontFamily:
    fontSize: 18,
  },
});
